
Question Link: https://www.geeksforgeeks.org/problems/check-if-a-number-is-divisible-by-83957/1

class Solution{
    int DivisibleByEight(String s){
         int length = s.length();
        int lastThreeDigits = 0;
        if (length >= 3) {
            lastThreeDigits = Integer.parseInt(s.substring(length - 3));
        } else {
            lastThreeDigits = Integer.parseInt(s);
        }

        if (lastThreeDigits % 8 == 0) {
            return 1; // Return 1 if divisible
        } else {
            return -1; // Return -1 if not divisible
        }
    }
}
