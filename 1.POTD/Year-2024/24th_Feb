/User function Template for Java

class Solution
{
    public int maxSum(int n) 
    { 
         if (n <= 1) {
            return n;
        }
        
        int partsSum = maxSum(n / 2) + maxSum(n / 3) + maxSum(n / 4);

        return Math.max(n, partsSum);
    } 
}
